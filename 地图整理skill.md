# Map Skill

只管地图，不管笔记内容。用户不介入整理。

目录结构：`00_地图.md` 放外层，原子笔记全放在 `./原子笔记/`。

每次新建 / 修改 / 删除原子笔记后，必须做：

1. 扫 `./原子笔记/*.md`，每篇只读 `# 标题` + `一句话理解` + `相关知识`，不读全文。
2. 重写外层的 `00_地图.md`，固定两块：按领域分、前置链条（链接指向 `./原子笔记/XX.md`）。
3. 文件名即 ID：一概念一文件，文件名 = `# 标题`，不带序号。
4. 前置链条从 `相关知识` 反推（被引用的在前）。
5. `相关知识` 只引用已存在的原子笔记；新概念写完成篇后再回来补链，不提前收录。
6. 只收纯数学原子笔记，不收项目应用；项目笔记另放别处，不进本目录。

------

## Map Quality Check

输出前检查：

-  每个链接都能点开（无悬空引用）；
-  每篇笔记在地图里出现且仅出现一次；
-  删掉的笔记不留在地图任何位置；
-  不读正文，不改笔记内容。
